package edu.project3.provider.sources;

/**
 * An interface representing a source of Nginx log entries.
 * Implementations of this interface define how to obtain log entries, whether from a file, a URL, or another source.
 */
public interface Source {
}
