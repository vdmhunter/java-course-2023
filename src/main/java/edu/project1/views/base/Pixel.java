package edu.project1.views.base;

/**
 * The {@code Pixel} record represents a single pixel on the screen with a character value and a text color.
 */
public record Pixel(char value, Color color) {
}
